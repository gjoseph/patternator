import { Polygons } from "./regular-polygons";
import { triangle } from "./triangles";

describe("Regular Polygons", () => {
  it.todo("regular polygon of 3 sides === equilateral triangle"); //, () => {
  // const expectedTriangle = triangle(10);
  // const polygon3 = Polygons.bySideLength(3, 10);
  // expect(polygon3).toMatchObject(expectedTriangle);
  // });
});
